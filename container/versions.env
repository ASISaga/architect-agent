# Declared versions for the Architect container
# Updating these records intent — actual upgrade happens when
# build-architect.yml is manually triggered in aos-infra.
# At session start, entrypoint.sh reports installed vs declared.

CLAUDE_CODE_VERSION=1.0.33
GH_CLI_VERSION=2.93.0
NODE_VERSION=22
